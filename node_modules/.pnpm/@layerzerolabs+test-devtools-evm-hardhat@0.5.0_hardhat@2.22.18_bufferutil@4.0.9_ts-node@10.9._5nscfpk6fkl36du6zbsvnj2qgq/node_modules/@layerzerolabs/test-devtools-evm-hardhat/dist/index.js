'use strict';

var context = require('hardhat/internal/context');
var runtimeEnvironment = require('hardhat/internal/core/runtime-environment');
var configLoading = require('hardhat/internal/core/config/config-loading');

// src/runtime.ts
var getTestHre = (args) => {
  const context$1 = context.HardhatContext.getHardhatContext();
  const environment = context$1.getHardhatRuntimeEnvironment();
  const hardhatArguments = { ...environment.hardhatArguments, ...args };
  const { resolvedConfig, userConfig } = configLoading.loadConfigAndTasks(hardhatArguments);
  return new runtimeEnvironment.Environment(
    resolvedConfig,
    hardhatArguments,
    environment.tasks,
    environment.scopes,
    context$1.environmentExtenders,
    userConfig,
    context$1.providerExtenders
    // This is a bit annoying - the environmentExtenders are not stronly typed
    // so TypeScript complains that the properties required by HardhatRuntimeEnvironment
    // are not present on HardhatRuntimeEnvironmentImplementation
  );
};

exports.getTestHre = getTestHre;
//# sourceMappingURL=out.js.map
//# sourceMappingURL=index.js.map