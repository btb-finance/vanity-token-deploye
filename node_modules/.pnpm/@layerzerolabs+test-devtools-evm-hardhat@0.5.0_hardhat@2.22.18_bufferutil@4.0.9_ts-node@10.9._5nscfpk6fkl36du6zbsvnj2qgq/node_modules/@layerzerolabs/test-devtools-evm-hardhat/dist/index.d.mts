import { HardhatArguments, HardhatRuntimeEnvironment } from 'hardhat/types';

/**
 * Creates a test HardhatRuntimeEnvironment with specific arguments
 *
 * ```typescript
 * const env = getTestHre({ networkName: "bsc-testnet", config: "./hardhat.config.other.ts" });
 * ```
 *
 * @returns {HardhatRuntimeEnvironment}
 */
declare const getTestHre: (args: Partial<HardhatArguments>) => HardhatRuntimeEnvironment;

export { getTestHre };
