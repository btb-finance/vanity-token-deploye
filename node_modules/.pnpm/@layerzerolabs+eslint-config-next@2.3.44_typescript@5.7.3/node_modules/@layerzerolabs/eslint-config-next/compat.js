// @ts-check
/** @type {import('eslint').ESLint.ConfigData} */

const eslintConfig = {
    plugins: ['compat'],
    extends: ['plugin:compat/recommended'],
    env: {
        browser: true,
    },
};

module.exports = eslintConfig;
