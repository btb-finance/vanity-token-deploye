declare const isDirectory: (path: string) => boolean;
declare const isFile: (path: string) => boolean;
declare const isReadable: (path: string) => boolean;
/**
 * CJS/ESM interoperable utility for importing JS/TS/JSON files
 *
 * This utility is not safe in a sense that it will not check for file existence
 * before trying to import it, consumers need to do that
 *
 * @param {string} path
 * @returns {unknown} imported module
 */
declare const importDefault: (path: string) => Promise<unknown>;

export { importDefault, isDirectory, isFile, isReadable };
